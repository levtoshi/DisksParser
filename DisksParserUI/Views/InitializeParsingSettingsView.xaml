<UserControl x:Class="DisksParserUI.Views.InitializeParsingSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DisksParserUI.Views" xmlns:converters="clr-namespace:DisksParserUI.Converters">
    <UserControl.Resources>
        <converters:StringToPathConverter  x:Key="StringToPathConverter"></converters:StringToPathConverter>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="120" Height="50" Command="{Binding GoToNextViewCommand}">
            <Button.Resources>
                <Style TargetType="Polygon" x:Key="TriangleStyle">
                    <Setter Property="Fill" Value="White"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=Button}}" Value="False">
                            <Setter Property="Fill" Value="Gray"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Resources>

            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="0" FontSize="24">Next</TextBlock>
                <Polygon Style="{StaticResource TriangleStyle}" Points="16,5 16,30 36,18"></Polygon>
            </StackPanel>
        </Button>

        <Grid Grid.Row="1" MinHeight="350" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="2.5*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Command="{Binding InitializeWordsCommand}">
                    <TextBlock FontSize="20">Initialize banned words</TextBlock>
                </Button>
                <Button Grid.Column="1" Command="{Binding InitializeFolderCommand}">
                    <TextBlock FontSize="20">Select folder to copy banned files</TextBlock>
                </Button>
            </Grid>

            <ScrollViewer Grid.Row="1" Margin="20">
                <TextBlock Margin="0" FontSize="20">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource StringToPathConverter}">
                            <Binding Path="PathToFileWithBannedWords"/>
                            <Binding Path="CopyFolderPath"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>